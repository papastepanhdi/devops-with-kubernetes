apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: the-project-step-4
  name: the-project-step-4
  namespace: default
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      app: the-project-step-4
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: the-project-step-4
    spec:
      containers:
        - image: papastepanhdi/devops-with-kubernetes:1.6
          ports:
            - containerPort: 8080
          imagePullPolicy: IfNotPresent
          name: devops-with-kubernetes
          resources: {}
          terminationMessagePath: /dev/termination-log
          terminationMessagePolicy: File
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: {}
      terminationGracePeriodSeconds: 30